#include<stdio.h>
#include<string.h>

int main()
{
    char a[1000];
    int i,j,k,ln=0;

      gets(a);

   j=0,i=0;
    ln=strlen(a);

   while(i != ln){

       if(a[i] !=' '){
         a[j++]=a[i];
       }

       i++;
   }
   a[j]=0;

   printf("%s\n",a);


    return 0;
}

